{
  "name": "/interrupt",
  "description": "Control Center: Quickly capture idea during active work",
  "protocol": {
    "entry_contract": "AI_START_HERE.md",
    "authority": "exact_capability_required_for_mutation",
    "egress": "separate_destination_specific_authorization",
    "default_role": "read_only"
  },
  "steps": [
    {
      "type": "ai",
      "action": "Prepare an interrupt proposal",
      "context": "Ask for the 1-2 sentence text and type. Show the exact append proposed for .exocortex/control/INTERRUPTS.md. Do not write or sync."
    },
    {
      "type": "user_choice",
      "options": [
        "A) Keep proposal only",
        "B) Request a one-time guarded interrupt-recording capability"
      ]
    },
    {
      "type": "ai",
      "action": "Respect the recording gate",
      "context": "If B, prepare the exact target path, work-item revision, executor, request ID, and content for a guarded append. Do not call capture_interrupt.sh without accepted authority."
    }
  ]
}
