{
  "name": "/check-keys",
  "aliases": [
    "check-keys",
    "keys"
  ],
  "description": "Validate all API keys — shows status, purpose, and location. Never exposes key values.",
  "protocol": {
    "entry_contract": "AI_START_HERE.md",
    "authority": "exact_capability_required_for_mutation",
    "egress": "separate_destination_specific_authorization",
    "default_role": "read_only"
  },
  "security": "NEVER read, echo, grep, or display any key value. Run the shell script only. Return its output verbatim.",
  "steps": [
    {
      "type": "shell",
      "command": "bash .exocortex/scripts/check_keys.sh",
      "description": "Checking all API keys (values never shown)",
      "success_format": "✓ Key check complete"
    },
    {
      "type": "ai",
      "command": "Display the shell script output verbatim. Do NOT read any .env file. Do NOT suggest showing key values. If any keys are invalid or missing, summarise the rotation steps in 2-3 lines using only the URLs already printed in the output.",
      "description": "Displaying results",
      "success_format": "✓ Results displayed"
    }
  ]
}
