{
  "name": "/init-exocortex",
  "description": "Bootstrap a new .exocortex directory for any project",
  "protocol": {
    "entry_contract": "AI_START_HERE.md",
    "authority": "exact_capability_required_for_mutation",
    "egress": "separate_destination_specific_authorization",
    "default_role": "read_only"
  },
  "steps": [
    {
      "type": "ai",
      "action": "Prepare a read-only clean-install plan",
      "context": "Do not create files. Identify the exact fresh target, pinned template SHA, expected paths, protected pre-existing files, fake HOME, and deterministic acceptance tests. Installation requires a separate work item, exact capability, and guarded installer rehearsal."
    }
  ]
}
