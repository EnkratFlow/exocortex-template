{
  "name": "/daily-end",
  "description": "End of day: reflect, capture what happened, close the day with a rich event",
  "protocol": {
    "entry_contract": "AI_START_HERE.md",
    "authority": "exact_capability_required_for_mutation",
    "egress": "separate_destination_specific_authorization",
    "default_role": "read_only"
  },
  "steps": [
    {
      "type": "ai",
      "action": "Prepare a project-local end-of-day report read-only",
      "context": "Use the current repository only: local Git metadata, existing local events, TODO, and interrupts. Do not scan or update another repository and do not read credential files."
    },
    {
      "type": "ai",
    "action": "Draft the event body in chat memory only",
    "context": "Draft a factual local narrative with accomplishments, decisions, problems, current state, and next work. Show it in chat. Do not create a file in /tmp or anywhere else. A chat draft is not a save or checkpoint."
    },
    {
      "type": "user_choice",
      "options": [
        "A) Keep the draft only",
        "B) Request a separately scoped guarded local-event recording operation",
        "C) Discard the draft"
      ]
    },
    {
      "type": "ai",
      "action": "Respect the selected authority boundary",
      "context": "Never sync automatically. If recording is requested, prepare the exact local event path and one-time capability request; do not write until the guarded executor accepts it."
    }
  ]
}
