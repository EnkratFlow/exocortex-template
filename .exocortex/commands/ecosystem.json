{
  "name": "/ecosystem",
  "description": "Cross-project activity view from EnkratFlow hub (read-only)",
  "protocol": {
    "entry_contract": "AI_START_HERE.md",
    "authority": "exact_capability_required_for_mutation",
    "egress": "separate_destination_specific_authorization",
    "default_role": "read_only"
  },
  "steps": [
    {
      "type": "ai",
      "action": "Display project-local ecosystem evidence when already present",
      "context": "Read only files beneath the current project .exocortex/hub directory. Do not read global config, credential files, sibling repositories, or an external hub. If local hub evidence is absent, say so."
    },
    {
      "type": "user_choice",
      "options": [
        "A) Continue current project — run /work",
        "B) Switch to another project",
        "C) Review a specific project",
        "D) Close"
      ]
    }
  ]
}
