**Read `.exocortex/AI_BOOTSTRAP.md` immediately.** It contains the complete command protocol, all 20 commands, memory system, and file structure for this project.

Then read `.exocortex/reference/MEMORY.md` for the project memory entry point.

**Then execute the /work workflow:** Run the steps in `.exocortex/commands/work.json` in order (see AI_BOOTSTRAP section 3, Step Execution Protocol). Run the shell steps, use their outputs for the AI step, then show the brief and the user_choice options.
